# Source and tooling — only the built `dist/` ships in the VSIX.
.vscode/**
src/**
docs/**
node_modules/**
# …except the Agent SDK, which is loaded at runtime via dynamic import()
# rather than bundled (it is ESM and uses import.meta.url).
!node_modules/@anthropic-ai/**
.gitignore
.eslintrc.json
esbuild.js
tsconfig.json
**/*.map
**/*.ts
**/*.tsx
atlas.example.yaml
